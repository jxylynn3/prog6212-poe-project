@model IEnumerable<ST10448420_CMCsystem.Models.Claims>

<style>
    /* 🌈 Legend Styling */
    .legend-container {
        display: flex;
        justify-content: center;
        flex-wrap: wrap;
        gap: 25px;
        margin: 1.5rem 0;
        font-weight: 600;
    }

    .legend-item {
        display: flex;
        align-items: center;
        gap: 8px;
        color: #344628;
    }

    .legend-color {
        width: 18px;
        height: 18px;
        border-radius: 4px;
        display: inline-block;
        border: 1px solid #ccc;
    }

    .pending-color {
        background-color: #C9CBA3;
    }

    .approved-color {
        background-color: #FFE1A8;
    }

    .rejected-color {
        background-color: #E26D5C;
    }

    .final-color {
        background-color: #723D46;
    }

    .badge {
        padding: 6px 12px;
        border-radius: 10px;
        font-weight: 600;
        font-size: 0.9rem;
    }

    .badge-pending {
        background-color: #C9CBA3;
        color: #344628;
    }

    .badge-approved {
        background-color: #FFE1A8;
        color: #344628;
    }

    .badge-rejected {
        background-color: #E26D5C;
        color: white;
    }

    .badge-final {
        background-color: #723D46;
        color: white;
    }

    .badge-default {
        background-color: #ccc;
        color: #333;
    }
</style>

@if (!Model.Any())
{
    <p class="text-muted text-center">No claims found in this category.</p>
}
else
{
    <div class="row">
        @foreach (var claim in Model)
        {
            <div class="col-md-4 mb-4">
                <div class="card shadow border-0 h-100">
                    <div class="card-body">
                        <h5>@claim.ClaimName</h5>
                        <p class="text-muted">@claim.ClaimDescription</p>
                        <p><strong>Lecturer:</strong> @claim.Lecturer?.FirstName @claim.Lecturer?.LastName</p>
                        <p><strong>Date:</strong> @claim.ClaimDate.ToShortDateString()</p>
                        <p><strong>Total:</strong> R@(claim.TotalHoursWorked* claim.HourlyRate)</p>


                        <span class="badge
            @(claim.ClaimStatus == "Pending" ? "badge-pending" :
                                    claim.ClaimStatus == "Approved by Coordinator" ? "badge-approved" :
                                    claim.ClaimStatus == "Rejected by Coordinator" ? "badge-rejected" :
                                    claim.ClaimStatus == "Final Approval / Rejection" ? "badge-final" : "badge-default")">
                    @claim.ClaimStatus
                </span>

            </div>

                    <!--  PENDING CLAIMS PART -->
            @if (claim.ClaimStatus == "Pending")
                    {
                        <div class="card-footer bg-white text-center">
                            <form asp-controller="Claims" asp-action="ApproveClaims" method="post" class="d-inline">
                                <input type="hidden" name="id" value="@claim.ClaimID" />
                                <button type="submit" class="btn btn-success btn-sm me-2">
                                    <i class="bi bi-check-circle"></i> Approve
                                </button>
                            </form>

                            <form asp-controller="Claims" asp-action="RejectClaims" method="post" class="d-inline">
                                <input type="hidden" name="id" value="@claim.ClaimID" />
                                <button type="submit" class="btn btn-danger btn-sm">
                                    <i class="bi bi-x-circle"></i> Reject
                                </button>
                            </form>

                            <a asp-controller="Claims" asp-action="ClaimDetails" asp-route-id="@claim.ClaimID"
                               class="btn btn-outline-info btn-sm mt-2 d-block">
                                <i class="bi bi-eye"></i> View Details
                            </a>
                        </div>
                    }

                    <!-- RE-REVIEW CLAIMS PART -->
                    @if (ViewData["Role"]?.ToString() == "AcademicManager" && claim.ClaimStatus == "Approved by Programme Coordinator")
                    {
                        <div class="card-footer bg-white text-center">
                            <form asp-controller="Claims" asp-action="ApproveClaims" method="post" class="d-inline">
                                <input type="hidden" name="id" value="@claim.ClaimID" />
                                <button type="submit" class="btn btn-success btn-sm me-2">
                                    <i class="bi bi-check-circle"></i> Approve
                                </button>
                            </form>

                            <form asp-controller="Claims" asp-action="RejectClaims" method="post" class="d-inline">
                                <input type="hidden" name="id" value="@claim.ClaimID" />
                                <button type="submit" class="btn btn-danger btn-sm">
                                    <i class="bi bi-x-circle"></i> Reject
                                </button>
                            </form>

                            <a asp-controller="Claims" asp-action="ClaimDetails" asp-route-id="@claim.ClaimID"
                               class="btn btn-outline-primary btn-sm mt-2 d-block">
                                <i class="bi bi-eye"></i> View Details
                            </a>
                        </div>
                    }
                </div>
            </div>
        }
    </div>
}
